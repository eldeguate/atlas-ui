# Leave unset on Vercel to serve the built-in mock (/api/ask).
R2R_BASE_URL=http://127.0.0.1:7272

# Bearer auth (POST /v2/login → /v2/rag). Use the ingest-owner user Codex created
# (not a fresh register — new users see 0 docs until granted access).
# R2R_USERNAME=<email from Codex Lane B setup>
# R2R_PASSWORD=<password from Codex Lane B setup>